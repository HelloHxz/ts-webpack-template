import AbstractRoute from '../../common/route';
import { RegisterRouteProperty } from '../../common/props';

class HistoryRoute extends AbstractRoute {
  static register = (props: RegisterRouteProperty): void => {};
}

export default HistoryRoute;
